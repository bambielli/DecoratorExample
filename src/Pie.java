/**
 * Abstract class that ties decorators and concrete objects together
 * with this common type.
 *
 * Could also be an interface instead of an abstract class.
 */
public abstract class Pie {
    public abstract double price();
}
